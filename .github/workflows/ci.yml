name: "CI - Minimal Hello"

on:
  workflow_dispatch:
  push:
    branches:
      - ci/**

jobs:
  hello:
    name: "Sanity Hello"
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: echo "hello from minimal ci.yml"